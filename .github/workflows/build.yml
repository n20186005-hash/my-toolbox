name: 工具列表自动更新与部署

on:
  push:
    branches:
      - main
  # 允许手动触发
  workflow_dispatch:

jobs:
  update-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read

    steps:
    - name: 检出代码仓库
      uses: actions/checkout@v4
      with:
        # 允许 Git 推送，需要 PAT_TOKEN
        token: ${{ secrets.PAT_TOKEN }}
        # 确保获取完整历史，对于后续的 Git Push 是必需的
        fetch-depth: 0 

    - name: 修复Git安全目录权限
      run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

    - name: 设置Python运行环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: ''  # 禁用自动缓存检测

    - name: 安装所需依赖
      run: |
        python -m pip install --upgrade pip

    # 这是新增的诊断步骤，用于确认文件是否被正确检出
    - name: 🚨 诊断：检查 modules 文件夹中的文件数量
      run: |
        # 递归查找 modules/ 目录下的所有 .html 文件并计数
        FILE_COUNT=$(find modules -name "*.html" | wc -l)
        echo "🚨 诊断发现的实际 HTML 文件数量：$FILE_COUNT"
        if [ "$FILE_COUNT" -lt 200 ]; then
          echo "⚠️ 警告：实际文件数量 ($FILE_COUNT) 小于预期 (200)。请检查 Git 提交状态。"
        fi
        
    - name: 运行工具列表生成脚本
      run: |
        echo "执行生成脚本..."
        # 检查 generate.py 是否存在于根目录
        if [ -f "generate.py" ]; then
          python generate.py || exit 1
          echo "✅ 生成脚本执行成功"
        else
          echo "❌ 找不到 generate.py 文件。请检查路径。"
          exit 1
        fi

    - name: 提交并推送变更
      run: |
        git config --global user.name "GitHub Action Bot"
        git config --global user.email "action-bot@github.com"
        
        # 检查是否有更改需要提交
        if git diff --exit-code --quiet; then
          echo "tools.json 和 details/ 目录没有变更，跳过提交。"
        else
          # 提交 tools.json 和 details 文件夹
          git add tools.json details
          git commit -m "chore: 🤖 [CI] Auto-update tools list and detail pages"
          
          # 尝试推送变更
          if ! git push; then
            echo "❌ Git推送失败，可能是权限问题或远程仓库没有变化"
            exit 1
          fi
          echo "✅ 变更已成功推送到仓库"
        fi
