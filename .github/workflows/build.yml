name: Update Tools and Deploy

# 触发条件：推送代码到 main 分支时执行
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    # 增加权限，避免推送时权限不足
    permissions:
      contents: write

    steps:
    # 1. 下载仓库代码
    - name: Checkout code
      uses: actions/checkout@v4

    # 2. 设置 Python 环境
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        cache: 'pip' # 缓存依赖，加快速度

    # 3. 安装可能需要的依赖（如果generate.py有依赖，比如requests，加在这里）
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # 如果有requirements.txt，取消下面注释
        # pip install -r requirements.txt

    # 4. 运行生成脚本，增加错误提示
    - name: Run generation script
      run: |
        echo "开始执行生成脚本..."
        if python scripts/generate.py; then
          echo "脚本执行成功！"
        else
          echo "❌ 生成脚本执行失败，请检查scripts/generate.py"
          exit 1
        fi

    # 5. 提交生成的文件到仓库（关键：用PAT_TOKEN，后续要配置）
    - name: Commit and Push changes
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        # 确保拉取最新代码，避免冲突
        git pull origin main --rebase
        # 添加生成的文件
        git add tools.json details/
        # 检查是否有变更，无变更则跳过提交
        if git diff --quiet && git diff --staged --quiet; then
          echo "✅ 无文件变更，跳过提交"
        else
          git commit -m "Auto-update tools list [GitHub Action]"
          echo "📤 推送变更到仓库..."
          git push https://${{ secrets.PAT_TOKEN }}@github.com/${{ github.repository }}.git main
        fi
      # 即使前面步骤有小问题，也尝试完成提交（可选）
      continue-on-error: false
